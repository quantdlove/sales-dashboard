/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable strict mode for React
  reactStrictMode: true,

  // Add other valid Next.js config options here if needed
};

module.exports = nextConfig;
